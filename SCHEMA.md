# SCHEMA.md

> Auto-generated by `npm run gen:schema`. Do not edit manually.
> Compact index — for column types/constraints, read the migration file.
> Legend: `*` = PRIMARY KEY · `!` = UNIQUE · `→table.col` = FOREIGN KEY

## Stats

- Tables: 8
- Total columns: 55
- Total indexes: 5

## Tables

### users (9 cols)
  id* · name · email! · avatar · membership_date · password · remember_me_token · created_at · updated_at

### sessions (5 cols)
  id* · user_id · user_agent · expires_at · created_at
  idx: user_id, expires_at

### password_reset_tokens (5 cols)
  id* · email · token! · created_at · expires_at
  idx: email

### assets (10 cols)
  id* · name · type · url · mime_type · size · s3_key · user_id · created_at · updated_at
  idx: user_id, s3_key

### roles (6 cols)
  id* · name · slug! · description · created_at · updated_at

### permissions (8 cols)
  id* · name · slug! · resource · action · description · created_at · updated_at

### role_permissions (6 cols)
  id* · role_id · permission_id · created_at · role_id→roles.id · permission_id→permissions.id

### user_roles (6 cols)
  id* · user_id · role_id · created_at · user_id→users.id · role_id→roles.id

## Relationships

```
users ──< sessions
users ──< password_reset_tokens
users ──< assets
users ──< user_roles >── roles
roles ──< role_permissions >── permissions
```

## Conventions

- IDs: `TEXT PRIMARY KEY` (UUID v4 via `crypto.randomUUID()`)
- Timestamps: `INTEGER` (Unix ms via `Date.now()`)
- Booleans: `INTEGER` (0/1) — SQLite.update() auto-converts
- Foreign keys: `ON DELETE CASCADE` unless noted
- Indexes on all foreign keys + frequently-searched columns
