# ROUTES.md

> Auto-generated by `npm run gen:routes`. Do not edit manually.
> Regenerate after adding/changing routes: `npm run gen:routes`.
> Read this to understand the API surface in one pass.

## Stats

- Total routes: 26
- Methods: GET: 14, POST: 8, PUT: 2, DELETE: 2

## Route List

### Public

- `GET` `/` → home.landingPage
### Auth

- `GET` `/login` → auth.loginPage
- `POST` `/login` → strictRateLimit(), auth.submitLogin
- `GET` `/register` → auth.registerPage
- `POST` `/register` → strictRateLimit(), auth.submitRegister
- `POST` `/logout` → strictRateLimit(), auth.logout
- `POST` `/change-password` `Auth` → auth.changePassword
### OAuth

- `GET` `/google/redirect` → strictRateLimit(), oauth.googleRedirect
- `GET` `/google/callback` → strictRateLimit(), oauth.googleCallback
### Protected - Users

- `GET` `/dashboard` `Auth` → users.dashboardPage
- `GET` `/users` `Auth` → users.usersPage
- `GET` `/profile` `Auth` → users.profilePage
- `POST` `/change-profile` `Auth` → users.changeProfile
- `POST` `/users` `Auth` → users.addUser
- `PUT` `/users/:id` `Auth` → users.editUser
- `DELETE` `/users` `Auth` → users.removeUsers
### Roles

- `GET` `/roles` `Auth` → roles.rolesPage
- `GET` `/roles/data` `Auth` → roles.listRoles
- `GET` `/roles/permissions` `Auth` → roles.permissionsData
- `POST` `/roles` `Auth` → roles.addRole
- `PUT` `/roles/:id` `Auth` → roles.editRole
- `DELETE` `/roles/:id` `Auth` → roles.removeRole
### Assets

- `POST` `/assets/avatar` `Auth, strictRateLimit, assets.avatarMiddleware` → assets.uploadAsset
- `GET` `/assets/:file` → assets.serveDistAsset
- `GET` `/public/*` → assets.servePublicAsset
- `GET` `/storage/*` → assets.servePublicAsset
